# The review also says "Missing updates to `colour-utils.js`".
# Oh! Wait! I previously patched `colour-utils.js` and then ran `git checkout HEAD creator-app.js index.html tracker-app.js stitch.html`.
# BUT I MIGHT HAVE RUN `git reset --hard` OR `git checkout HEAD .` ??
# No, let's check `colour-utils.js`.
