# Stats Inventory — All Data Displays Across the App

Generated by audit pass on every screen, panel, modal, tooltip, and widget.

---

## 1. Stats Page (`stats-page.js` — `GlobalStatsDashboard`)

### 1.1 Stitching tab

| # | What it shows | File / line | Data source | Calculation | Render type | Updates when |
|---|---|---|---|---|---|---|
| S1 | Lifetime Stitches (hero) | stats-page.js ~L90 | `ProjectStorage.getLifetimeStitches()` | Sum of all `statsSessions[].netStitches` across all projects | Large text via `fmtNum()` + "≈ N km of thread" | On mount `useEffect([])` only |
| S2 | Active Projects count | ~L120 | `projectDetails` state | `filter(p => finishStatus === 'active').length` | Text number | On mount `useEffect([])` only |
| S3 | Finished This Year | ~L130 | `projectDetails` | `filter(completedAt >= yearStart).length` | Text number | On mount only |
| S4 | Coverage Ratio | ~L1060 | MULTI-SOURCE: stash + all project patterns + DMC catalog + dE2000 | Complex async sub-routine `computeCoverage` | `RadialGauge` SVG + "N%" text | After mount async (no refresh) |
| S5 | Weekly Streak (current) | ~L155 | `richProjects.statsSessions` | `InsightsEngine.computeWeeklyStreak` | Text number | `useMemo` from `richProjects` (load-once) |
| S6 | Weekly Streak (longest) | ~L160 | Same | Same | Text number | Same |
| S7 | Recent Pace | ~L165 | `richProjects.statsSessions` | Rolling average stitches/day | Text | `useMemo` from `richProjects` |
| S8 | SABLE sentence | ~L1450 | `sableData` from `StashBridge.getAcquisitionTimeseries(12)` | Computed sentence | Text | `useMemo` from `sableData` (load-once) |
| S9 | SABLE chart | ~L260 | Same | Monthly buckets | `SableChart` SVG polyline | Load-once |
| S10 | Colour Families wheel | ~L300 | MULTI-SOURCE: `stash` × `findThreadInCatalog` × `classifyColourFamily` | Group-by hue family | SVG donut + legend chips | `useMemo` from `stash` (load-once) |
| S11 | HueWheel (12-bin) | ~L1325 | `stash` × LAB colour bins | 12-bucket hue grouping | `HueWheel` SVG | `useMemo` from `stash` (load-once) |
| S12 | DMC Palette Coverage | ~L1340 | `stash` DMC-only / `DMC.length` | Count owned DMC / total DMC | `RadialGauge` + "N/M" text | `useMemo` from `stash` (load-once) |
| S13 | Ready to Start list | ~L1910 | `ProjectStorage.getProjectsReadyToStart()` | pattern threads vs. stash owned | Count + clickable list | Load-once |
| S14 | Use What You Have recs | ~L2065 | MULTI-SOURCE: `neverUsedData` × `managerPatterns` × stash | Cross-reference stash × wishlist | Pattern name + dormant thread count | `useMemo` from neverUsedData + managerPatterns |
| S15 | Buying Impact | ~L2077 | MULTI-SOURCE: `managerPatterns` × stash coverage gap | Thread→pattern cross-reference | Swatch + "DMC N · name" + "unlocks N" | `useMemo` |
| S16 | Duplicate Alerts | ~L1931 | `stash.history` grouped by id | Repeat adds or near-match dE2000 | `Swatch` + "DMC N" text | `useMemo` from `stash` |
| S17 | Oldest WIPs (top-5) | ~L1954 | `ProjectStorage.getOldestWIPs(5)` | Sort by updatedAt | Name + "Nd · N%" text | Load-once |
| S18 | Stash Age bar | ~L1985 | `StashBridge.getStashAgeDistribution()` | Bucket by add-date | `AgeBar` stacked bar + legend | Load-once |
| S19 | Most-Used Colours list | ~L2000 | MULTI-SOURCE: `ProjectStorage.getMostUsedColours(10)` + `stash` | Stitch count across all projects | `Swatch` chips + ownership badge | Load-once |
| S20 | Threads Never Used | ~L2040 | MULTI-SOURCE: `stash` × all project patterns | Set difference: stash keys not in any project | Count text + swatch row (24×24 divs) | `useEffect([stash, loading])` |
| S21 | Colour Fingerprint | ~L2090 | MULTI-SOURCE: `mostUsed` × `stash` | Jaccard similarity, top-5 overlap/gap | Percentage bar + **plain ID lists** | `useMemo` from `mostUsed` + `stash` |
| S22 | Designer Leaderboard | ~L2110 | `richProjects` | Group by `designerName` | Ranked list: name + total + finished | `useMemo` from `richProjects` |
| S23 | Brand Alignment | ~L2120 | MULTI-SOURCE: `managerPatterns` × `stash` brand preference | Pattern brand vs. stash brand | "N%" text + conflict list | `useMemo` |
| S24 | Quarter Portfolio | ~L2137 | `richProjects` | Quarter bucket by started/finished date | `QuarterAreaChart` SVG | `useMemo` |
| S25 | Difficulty vs Completion | ~L2143 | `richProjects` | `calcDifficulty()` × `pct` | `DifficultyScatter` SVG | `useMemo` |
| S26 | Pattern Source | ~L2047 | All projects | `w×h` size buckets | Bar chart | `useEffect([])` once |

### 1.2 Stash tab (within `GlobalStatsDashboard`)

*(The Stash tab content is delegated to child components; see Section 2 for the main stats section within stats-page.js.)*

### 1.3 Showcase tab (`StatsShowcase`)

| # | What it shows | File / line | Data source | Render type | Updates when |
|---|---|---|---|---|---|
| SC1 | Lifetime Stitches hero | ~L840 | `ProjectStorage.getLifetimeStitches()` | 64px `<h2>` + "≈ N km" sub | Load-once `useEffect([])` |
| SC2 | Oldest stash thread | ~L996 | `ageData.oldest.name + addedAt` | **Plain text** — no colour swatch | Load-once |
| SC3 | Ready-to-start patterns | ~L974 | `readyToStart.filter(pct>=100)` | `PatternChip` — title + "N/N threads ready" text only, **no colour swatches** | Load-once |
| SC4 | Stash Age bar | ~L1000 | `StashBridge.getStashAgeDistribution()` | `ShowcaseAgeBar` stacked bar | Load-once |
| SC5 | SABLE chart | ~L950 | `StashBridge.getAcquisitionTimeseries(12)` | `SableLineChart` SVG | Load-once |
| SC6 | Stash total count | ~L1000 | `sum(ageData buckets)` | "N threads in your stash." text | Load-once |

---

## 2. Home Page (`home-app.js`)

### 2.1 Projects tab

| # | What it shows | File / line | Data source | Render type | Updates when |
|---|---|---|---|---|---|
| H1 | Project count badge | home-app.js ~L50 | `list.length` from `ProjectStorage.listProjects()` | Badge number in tab | On mount + `cs:projectsChanged` |
| H2 | "N projects" greeting | ~L60 | Same | Text | On mount |
| H3 | Pattern dimensions "W×H" | ~L200 | `p.settings.sW × p.settings.sH` | Text | On mount |
| H4 | "Updated X ago" | ~L210 | `timeAgo(p.updatedAt)` | Text | On mount |
| H5 | "N% complete" | ~L215 | `projectPct(p)` — scans `p.pattern` + `p.done` | Text + progress fill | On mount |
| H6 | ETA label "Est. Nov 2026" | ~L220 | `etaLabel(stitchesPerHour, done, total)` | Text (conditional on `totalTime>0 && done>50`) | On mount |
| H7 | Metadata popover: dimensions | ~L350 | `p.settings.sW + '×' + p.settings.sH` | Text | On popover open |
| H8 | Metadata popover: stitchable count | ~L360 | Scan `p.pattern` | Text | On popover open |
| H9 | Metadata popover: colours count | ~L365 | Distinct IDs in `p.pattern` | Text | On popover open |
| H10 | Metadata popover: progress | ~L370 | `projectPct()` + done/stitchable | Text "N% (done/total)" | On popover open |
| H11 | Metadata popover: Time Spent | ~L375 | `p.totalTime` → `fmtTimeL()` | Text | On popover open |

### 2.2 Stash tab (`StashPanel`)

| # | What it shows | File / line | Data source | Render type | Updates when |
|---|---|---|---|---|---|
| H12 | Skeins KPI | home-app.js ~L920 | `StashBridge.getGlobalStash()` — sum of `.owned` | Large number text | `refreshStash()` — called on **tab switch to 'stash'** only |
| H13 | Colours KPI | ~L925 | `uniqueThreads` — distinct owned keys | Large number text | Same |
| H14 | Patterns KPI | ~L930 | `manager_state/patterns` array length | Large number text | Same |
| H15 | Shopping list items | ~L950 | `StashBridge.getShoppingList()` | Colour swatch div + "DMC N — name" text | Same |
| H16 | Ready-to-start chips | ~L960 | `ProjectStorage.getProjectsReadyToStart()` | Pattern title chip only (text) | Same |

### 2.3 Stats tab (`StatsPanel`)

| # | What it shows | File / line | Data source | Render type | Updates when |
|---|---|---|---|---|---|
| H17 | Lifetime stitches hero | home-app.js ~L1200 | `ProjectStorage.getLifetimeStitches()` | Large text + "≈ N km" sub | On tab switch to 'stats' |
| H18 | 30-day sparkline | ~L1220 | `buildSpark(dailyLog, 30)` | SVG 120×32 polyline + "N stitches · N active days" | On tab switch |
| H19 | Oldest WIP | ~L1230 | `ProjectStorage.getOldestWIP()` | Name + "Last touched X · N% done" text | On tab switch |

---

## 3. Tracker App (`tracker-app.js`)

### 3.1 Always-visible progress strip

| # | What it shows | File / line | Data source | Render type | Updates when |
|---|---|---|---|---|---|
| T1 | Progress bar | tracker-app.js ~L3000 | `effectiveCombinedDone / effectiveCombinedTotal` | Dual-segment bar | Every stitch toggle |
| T2 | "N.N%" or "Complete!" | Same | Same | Text | Every stitch toggle |
| T3 | Live session timer | ~L3050 | `liveAutoElapsed` real-time ref | Text "Xh Ym" | Every second |

### 3.2 Progress info chip popover

| # | What it shows | File / line | Data source | Render type | Updates when |
|---|---|---|---|---|---|
| T4 | "Done: N / M (N.N%)" | tracker-app.js ~L580 | `effectiveCombinedDone / effectiveCombinedTotal` | Text | On chip click (re-renders) |
| T5 | "Today: N" | ~L585 | `todayStitchesForBar` | Text `.toLocaleString()` | Same |
| T6 | "This week: N" | ~L590 | `weekStitchesForChip` | Text | Same |
| T7 | "Time spent: Xh" | ~L595 | `totalTime` → `fmtTimeL()` | Text | Same |
| T8 | "Average pace: N st/hr" | ~L600 | `doneCount / (totalTime/3600)` | Text | Same |
| T9 | "Remaining: Xh" | ~L605 | `remainingStitches / speedPerHour * 3600` | Text | Same |

### 3.3 Session complete modal

| # | What it shows | Data source | Render type |
|---|---|---|---|
| T10 | Time "Nm Ns" | `durationSeconds` | Text |
| T11 | Stitches completed | `stitchesCompleted` | Text |
| T12 | Speed "N st/hr" + delta | `stitchesCompleted / (duration/3600)` vs `prevAvgSpeed` | Text + coloured badge |
| T13 | Progress "N% → N%" | `progressPctBefore → progressPctAfter` | Text + "+N%" badge |
| T14 | Colours finished | `coloursCompleted.length` | Text |

### 3.4 Tracker Project Rail (sidebar)

| # | What it shows | File / line | Data source | Render type | Updates when |
|---|---|---|---|---|---|
| T15 | Project list thumbnails | tracker-app.js ~L437 | `ProjectStorage.listProjects().slice(0,8)` | Thumb/initial | On mount + `cs:projectsChanged` |
| T16 | Per-project "N% done" | ~L450 | `p.progressPct` | Text | Same |
| T17 | "Threads needed · N" | ~L510 | `skeinData.length` | Text count | `useMemo` from `pal` |
| T18 | "To buy · N" | ~L515 | `needRows.length` — threads not in stash | Text count | `useMemo` |
| T19 | "In stash · N" | ~L520 | `ownedRows.length` | Text count | `useMemo` |
| T20 | Per-thread colour swatch | ~L535 | `d.rgb` | 10×10 `tsp-sw` div | `useMemo` |
| T21 | Per-thread DMC id + name | ~L540 | `d.id`, `d.name` | Text | `useMemo` |
| T22 | Per-thread "N×" skeins needed | ~L545 | `d.skeins` | Text | `useMemo` |
| T23 | Stash ownership pip | ~L535 | `owned >= d.skeins` → check icon or count | Icon or text | `useMemo` — **loaded once on mount** |

---

## 4. Stash Manager (`manager-app.js`)

### 4.1 Tab bar badges

| # | What it shows | Data source | Render type | Updates when |
|---|---|---|---|---|
| M1 | Thread Stash count badge | `Object.values(threads).reduce((s,t) => s + t.owned, 0)` | Number in `.cnt` span | On every `setState` in Manager |
| M2 | Pattern Library count badge | `patterns.length` | Number | On every `setState` |

### 4.2 Inventory smart chips

| # | What it shows | Data source | Render type |
|---|---|---|---|
| M3 | "N skeins · N low" chip summary | `totalOwnedCount`, `lowStockNeeded.length` | Text in `.mgr-stash-chip-summary` |
| M4 | Popover: Total skeins | `totalOwnedCount.toLocaleString()` | `AppInfoGrid` text row |
| M5 | Popover: Distinct threads | Pass through thread keys | `AppInfoGrid` text row |
| M6 | Popover: Low stock (needed) | `lowStockNeeded.length` | Status row |
| M7 | Popover: Conflicts | `conflicts.length` | Status row |
| M8 | Popover: Ready to start | `readyToStart.length` | Status row |

### 4.3 Smart Hub alert cards

| # | What it shows | Data source | Render type |
|---|---|---|---|
| M9 | Thread Conflicts | MULTI-SOURCE: `StashBridge.detectConflicts()` | 14×14 swatch + text |
| M10 | Low-stock (needed) | MULTI-SOURCE: thread × patterns × owned | 14×14 swatch + text |

### 4.4 Thread grid

| # | What it shows | Data source | Render type |
|---|---|---|---|
| M11 | Colour swatch | `d.rgb` | `sw` div with `background: rgb(${d.rgb})` |
| M12 | Thread ID | `d.id` | Text |
| M13 | Thread name | `d.name` | Text |
| M14 | "Low" badge | `owned > 0 && owned <= lowStockThreshold` | `.badge-low` span |
| M15 | Owned count | `state.owned` | Text in `.owned` |
| M16 | Partial gauge | `state.partialStatus` mapped to 0-4 fill | `PartialGauge` 4-segment bar |

### 4.5 Thread detail panel

| # | What it shows | Data source | Render type |
|---|---|---|---|
| M17 | Large colour swatch | `d.rgb` | `td-swatch` div |
| M18 | "Brand N" + name | `d.id`, `d.name` | Text |
| M19 | "Used in" pattern list | `patternsUsingThread(d.id)` | Pattern title + "need N sk" text (**no thread swatch**) |

### 4.6 Patterns tab

| # | What it shows | Data source | Render type |
|---|---|---|---|
| M20 | "N patterns" count | `patterns.length` | Text |
| M21 | "N fully kitted" | `readyToStart.filter(r => r.pct === 100).length` | Text |
| M22 | Thread Coverage % | MULTI-SOURCE: `p.threads` × `threads` stash | Percentage + `<h2>` badge + 6px progress bar |
| M23 | Missing threads list | `missingThreads` × `DMC.find(id)` | 12×12 swatch + text "id name need Nsk" |
| M24 | "N% stitched" progress badge | MULTI-SOURCE: `completed/total*100` from `storedProjects` meta | Coloured badge |
| M25 | 7-day sparkline | `meta.weeklyStitches[7]` from project meta | SVG 7-bar sparkline |
| M26 | "Fully kitted" or "N threads needed" | MULTI-SOURCE: `pat.threads` × `threads` stash | Text badge |

---

## 5. Pattern Creator (`creator/`)

### 5.1 Legend tab (`creator/LegendTab.js`)

| # | What it shows | File / line | Data source | Render type | Updates when |
|---|---|---|---|---|---|
| C1 | "N colours, N skeins" | LegendTab.js ~L40 | `rows` useMemo | Text | On `pal`/stash change |
| C2 | "Stash: N/N owned" | ~L45 | `ownedColours / totalColours` | Text | Same |
| C3 | "N partial" | ~L48 | `partialColours` | Text | Same |
| C4 | "~N skeins still needed" | ~L50 | `sum(max(0, needed - owned))` | Text | Same |
| C5 | Per-thread: colour swatch | ~L180 | `p.rgb` | 20×20 div | On render |
| C6 | Per-thread: DMC code | ~L185 | `p.id` | Bold text `<td>` | On render |
| C7 | Per-thread: name | ~L188 | `p.name` | Text | On render |
| C8 | Per-thread: stitch count | ~L195 | `p.count` | Right-aligned text | On render |
| C9 | Per-thread: skeins needed | ~L198 | `stitchesToSkeins()` or `skeinEst()` | Right-aligned text | `useMemo` |
| C10 | Per-thread: In stash count | ~L200 | `stash[threadKey('dmc', p.id)].owned` | Right-aligned text | `useMemo`, re-runs on stash change |
| C11 | Per-thread: status badge | ~L205 | `r.status` (owned/partial/needed) | Coloured text badge | Same |
| C12 | Per-thread: Done "done/total" | ~L208 | `ctx.colourDoneCounts[p.id]` | Text (conditional on tracking) | On render |
| C13 | Confetti count per colour | ~L212 | `app.confettiData.clean.colorConfetti[p.id]` | Warning text | On confetti data change |
| C14 | Fabric size table | ~L220 | `ctx.sW / fabricCount` math | Text "N.N″ / N.N cm" × 7 types | On render |

### 5.2 Pattern tab (`creator/PatternTab.js`)

| # | What it shows | File / line | Data source | Render type | Updates when |
|---|---|---|---|---|---|
| C15 | Stitch Score "N/100" | PatternTab.js ~L80 | `100 - app.confettiData.clean.pct` | Text + 5px progress bar | On `confettiData` change |
| C16 | "N isolated stitches remaining" | ~L85 | `app.confettiData.clean.singles` | Text | Same |
| C17 | Status bar: DMC code + name | ~L150 | `ctx.cmap[cell.id]` | "DMC N name (N st)" text + 8×8px swatch | On cell hover |

### 5.3 Project tab (`creator/ProjectTab.js`)

| # | What it shows | File / line | Data source | Render type | Updates when |
|---|---|---|---|---|---|
| C18 | Time Estimate: Total | ProjectTab.js ~L40 | `ctx.totalStitchable / ctx.stitchSpeed * 3600` | Text `fmtTimeL()` | On speed/total change |
| C19 | Time Estimate: Remaining | ~L45 | `(totalStitchable - doneCount) / stitchSpeed * 3600` | Text | On state change |
| C20 | "Based on actual sessions: N st/hr" | ~L50 | `ctx.doneCount / (ctx.totalTime/3600)` | Text | On render |
| C21 | Finished Size table | ~L70 | `ctx.sW / fabricCount * 2.54` | Text "N.N″ / N.N cm" × 7 | On `sW`/`sH` change |
| C22 | Thread cost "£N.NN" | ~L120 | `ctx.totalSkeins * ctx.skeinPrice` | Text | On skeinPrice/skeins change |
| C23 | "Still to buy: £N.NN (N skeins)" | ~L125 | `ctx.toBuyList.reduce(skeins) * skeinPrice` | Text | On stash/skeins change |
| C24 | Thread Organiser: per-thread | ~L140 | `ctx.skeinData` × `ctx.globalStash` | 16×16 swatch + text + stash badge | On `globalStash` change |
| C25 | Kitting check result | ~L160 | `StashBridge.getGlobalStash()` comparison | "Missing (N):" list — **text IDs only**, no swatch | On "Kit This Project" click |
| C26 | Alternatives popup | ~L170 | `StashBridge.suggestAlternatives(id, 5, stash)` | 10×10 swatch + "DMC N name ΔE N" chips | On "≈" button click |

---

## 6. Stats Activity (`stats-activity.js`)

| # | What it shows | File / line | Data source | Render type | Updates when |
|---|---|---|---|---|---|
| A1 | Activity Heatmap | stats-activity.js ~L200 | All projects' `statsSessions` | SVG grid, 5-level bins | On mount only |
| A2 | Heatmap tooltip | ~L220 | `byDay[date]` | Overlay div text | On hover |
| A3 | Avg per Session | ~L280 | `totalStitches / active.length` | `fmtNum()` text | `useMemo` from grid |
| A4 | Est. Hours | ~L285 | `totalSec/3600` or `totalStitches/400` | Text | `useMemo` |
| A5 | Avg Session duration | ~L290 | `estHours / active.length` | Text "~Xh Ym" | `useMemo` |
| A6 | Active Days | ~L295 | `sessionCount / totalDays` | Text "N of M in period" | `useMemo` |
| A7 | Favourite Day | ~L310 | `dowTotals` from session data | Text insight card | `useMemo` |
| A8 | Busiest Weeks/Months | ~L330 | Grouped from session grid | Ranked list + `Sparkline` SVG | `useMemo` |

---

## 7. Stats Insights (`stats-insights.js`)

| # | What it shows | File / line | Data source | Render type | Updates when |
|---|---|---|---|---|---|
| I1 | Weekly Summary text | stats-insights.js ~L210 | `InsightsEngine.generateWeeklySummary` from `allSessions` | Prose text card | `useMemo` from `allSessions` (load-once) |
| I2 | ProjectionCard per project | ~L230 | `InsightsEngine.generateProjections(summaries)` | Name + progress bar + projected date | `useMemo` from `summaries` |
| I3 | InsightCard | ~L260 | `InsightsEngine.generateInsights({...})` | Dismissable text card | `useMemo` |
| I4 | ColourHeatmap | ~L300 | MULTI-SOURCE: `getMostUsedColours(50)` + `getGlobalStash()` | 24×24 swatch grid + ownership indicator + tooltip | On mount load |
| I5 | RhythmHeatmap | ~L340 | `InsightsEngine.buildRhythmMatrix(allSessions)` | 7×24 grid heatmap | `useMemo` from allSessions |

---

## Summary of Key Data Characteristics

### Multi-source stats (most risk of staleness)
- S4 Coverage Ratio (stash + projects + catalog)
- S10, S11, S12 Colour Families / HueWheel / DMC Coverage (stash)
- S14 Use What You Have (stash × wishlist patterns)
- S15 Buying Impact (stash × wishlist patterns)
- S21 Colour Fingerprint (mostUsed × stash)
- S23 Brand Alignment (managerPatterns × stash)
- S19 Most-Used + stash ownership badge
- T17–T23 Tracker side panel threads (skeinData × globalStash)
- M9, M10 Manager Smart Hub (stash × patterns)

### Color displays present but potentially small/inadequate
- C17 PatternTab status bar swatch: only 8×8px
- SC2 Showcase oldest stash thread: no colour swatch
- SC3 Showcase ready-to-start: no thread colour swatches

### Bare IDs shown as text (confirmed no swatch)
- S21 Colour Fingerprint `usedNotOwned` / `ownedNotUsed` lists: plain string IDs
- C25 Kitting check "Missing" list in ProjectTab: "DMC N (need Nsk)" text, no swatch
